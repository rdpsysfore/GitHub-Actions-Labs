name: Release Package

on:
  release:
    types:
      - published

jobs:
  package:
    runs-on: ubuntu-latest

    steps:

      - run: |
          echo "Application Package" > app.txt

      - uses: actions/upload-artifact@v4
        with:
          name: release-package
          path: app.txt
