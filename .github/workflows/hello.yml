name: Expression Lab

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "User: ${{ github.actor }}"
          echo "Repository: ${{ github.repository }}"
