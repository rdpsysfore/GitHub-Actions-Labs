name: Environment Matrix

on:
  workflow_dispatch:

jobs:
  deploy:
    strategy:
      matrix:
        environment:
          - dev
          - uat
          - prod

    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Deploying to ${{ matrix.environment }}"
          
