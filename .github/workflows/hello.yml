name: Environment Variables Lab

on:
  workflow_dispatch:

env:
  APP_NAME: GitHubActionsLab
  ENVIRONMENT: Dev
  OWNER: DPK

jobs:
  env-demo:
    runs-on: ubuntu-latest

    steps:
      - name: Display Variables
        run: |
          echo "Application: $APP_NAME"
          echo "Environment: $ENVIRONMENT"
          echo "Owner: $OWNER"
