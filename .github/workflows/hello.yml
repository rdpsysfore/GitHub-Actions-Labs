name: Input Condition Lab

on:
  workflow_dispatch:
    inputs:
      deploy:
        description: "Deploy Application?"
        required: true
        type: boolean

jobs:
  deploy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        if: github.event.inputs.deploy == 'true'
        run: |
          echo "Deploying Application"

          
