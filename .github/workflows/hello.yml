name: Java Matrix

on:
  workflow_dispatch:

jobs:
  java-test:
    strategy:
      matrix:
        java:
          - 17
          - 21

    runs-on: ubuntu-latest

    steps:
      - name: Display Java Version
        run: |
          echo "Testing Java ${{ matrix.java }}"
