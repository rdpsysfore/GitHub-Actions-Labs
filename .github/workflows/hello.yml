name: Secrets Lab

on:
  workflow_dispatch:

jobs:
  secret-demo:
    runs-on: ubuntu-latest

    steps:
      - name: Read Secret
        run: |
          echo "Secret Loaded Successfully"

        env:
          MY_SECRET: ${{ secrets.TEST_SECRET }}
