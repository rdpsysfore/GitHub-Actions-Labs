name: Version Info

on:
  workflow_dispatch:

jobs:
  version:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Repository:"
          echo "${{ github.repository }}"

          echo "Run Number:"
          echo "${{ github.run_number }}"
